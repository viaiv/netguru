# URL base da API para builds de produção/staging.
# Defina no ambiente de deploy (ex.: https://api.seu-dominio.com).
VITE_API_URL=

# Apenas no desenvolvimento local:
# se VITE_API_URL estiver vazio, o Vite lê UVICORN_PORT desse arquivo.
DEV_BACKEND_ENV_FILE=../backend/.env
